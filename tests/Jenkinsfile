pipeline {
    agent { 
        dockerfile {
            dir 'tests'
            filename 'Dockertest'
        }
    }
    stages {
        stage('Unit Tests') {
            steps {
                sh 'echo hello'
            }
        }
    }
}

